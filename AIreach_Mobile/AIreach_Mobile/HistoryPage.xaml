<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AIreach_Mobile.HistoryPage"
             BackgroundColor="#F5F7FA"
             Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="Auto, *">
        <Grid Grid.Row="0" Padding="25" ColumnDefinitions="*, Auto">
            <VerticalStackLayout Grid.Column="0">
                <Label Text="Geçmiş" FontSize="32" FontAttributes="Bold" TextColor="#111827"/>
                <Label Text="Son analizlerin burada." FontSize="14" TextColor="#6B7280"/>
            </VerticalStackLayout>
            <Button Grid.Column="1" Text="🗑️" Clicked="OnClearClicked" BackgroundColor="#FEE2E2" TextColor="#EF4444" HeightRequest="45" WidthRequest="45" CornerRadius="12"/>
        </Grid>

        <CollectionView x:Name="cvHistory" Grid.Row="1" Margin="25,0,25,25">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="15"/>
            </CollectionView.ItemsLayout>
            <CollectionView.EmptyView>
                <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Spacing="10" Padding="0,50,0,0">
                    <Label Text="🕰️" FontSize="40" HorizontalOptions="Center"/>
                    <Label Text="Geçmiş temiz." TextColor="#9CA3AF" HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </CollectionView.EmptyView>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border StrokeShape="RoundRectangle 15" StrokeThickness="0" BackgroundColor="White" Padding="20">
                        <Border.Shadow>
                            <Shadow Brush="Black" Opacity="0.05" Offset="0,4" Radius="8"/>
                        </Border.Shadow>
                        <VerticalStackLayout Spacing="8">
                            <Grid ColumnDefinitions="*, Auto">
                                <Label Grid.Column="0" Text="{Binding Baslik}" FontSize="16" FontAttributes="Bold" TextColor="#2563EB"/>
                                <Label Grid.Column="1" Text="{Binding Tarih}" FontSize="12" TextColor="#9CA3AF"/>
                            </Grid>
                            <Label Text="{Binding Yazar}" FontSize="13" FontAttributes="Italic" TextColor="#4B5563"/>
                            <BoxView HeightRequest="1" Color="#F3F4F6"/>
                            <Label Text="{Binding Analiz}" FontSize="14" TextColor="#374151" MaxLines="3" LineBreakMode="TailTruncation"/>
                        </VerticalStackLayout>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>